require 'spec_helper'

describe WordExchanger do
  it 'has a version number' do
    expect(WordExchanger::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
